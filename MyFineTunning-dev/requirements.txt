# ==============================================================================
# requirements.txt — MyFineTunning
# ==============================================================================
# RunPOD sudah pre-install torch+CUDA — cukup install package tambahan.
# Jalankan via: bash setup.sh
# ==============================================================================

# YOLO + SAM2 (otomatis install torch-compatible)
ultralytics

# Boundary IoU API (Cheng et al., CVPR 2021) — auto-install via eval_boundary_iou.py
# Manual: pip install git+https://github.com/bowenc0221/boundary-iou-api.git
# boundary-iou-api

# Image processing
opencv-python-headless>=4.9.0
Pillow>=10.2.0

# Data & plotting
numpy>=1.26.0
matplotlib>=3.8.0
pyyaml>=6.0.1

# Utility
tqdm>=4.66.0
nvitop
pycocotools

# Dataset management
roboflow
python-dotenv
